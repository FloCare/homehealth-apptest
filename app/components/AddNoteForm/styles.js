import {PrimaryColor} from '../../utils/constants';

const styles = {
    buttonStyle: {
        backgroundColor: PrimaryColor,
        marginLeft: 0,
        marginRight: 0,
        height: 50
    }
};

export default styles;
