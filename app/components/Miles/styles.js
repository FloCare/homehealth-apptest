import {StyleSheet} from 'react-native';

const grayColor = '#999999';

export const styles = StyleSheet.create({
    inputTitleStyle: {
        color: grayColor,
        fontSize: 12,
        marginBottom: 5
    }
});
